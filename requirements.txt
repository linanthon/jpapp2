typer==0.16.0
argparse==1.4.0

# Scarpe data
beautifulsoup4==4.13.4
requests==2.32.4

# DB
psycopg2-binary==2.9.10

# tokenize words and dict
fugashi==1.5.1
unidic-lite==1.0.8
jamdict==0.1a11.post2
jamdict-data==1.5
# mecab-python3==1.0.10       # only tokenize

# Split Kanji into Katakana
jamorasep==0.0.1
typeguard==4.4.4

# Play audio (for cli)
# miniaudio==1.61       # can play with both bytes and files
# playsound==1.3.0      # can only play with file
# pydub==0.25.1         # requires ffmpeg
# simpleaudio==1.0.4    # setup.py bdist_wheel is deprecated, can't build
# sounddevice==0.5.2    # read audio file into ndarray, aka. needs numpy
# soundfile==0.13.1     # requires numpy
# scipy==1.16.0         # requires numpy

# Web
flask==3.1.1